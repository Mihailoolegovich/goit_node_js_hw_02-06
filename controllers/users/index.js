const register = require("./register");
const login = require("./login");
const logout = require("./logout");
const current = require("./current");
const patchUser = require("./patch");

module.exports = { register, login, logout, current, patchUser };
